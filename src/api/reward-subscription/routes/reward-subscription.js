'use strict';

/**
 * reward-subscription router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::reward-subscription.reward-subscription');
