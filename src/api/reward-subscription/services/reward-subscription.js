'use strict';

/**
 * reward-subscription service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::reward-subscription.reward-subscription');
